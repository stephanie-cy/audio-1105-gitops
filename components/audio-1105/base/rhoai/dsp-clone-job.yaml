apiVersion: batch/v1
kind: Job
metadata:
  name: dsp-clone-job-audio-1105-gitops
spec:  
  template:         
    metadata:
      name: dsp-clone-job-audio-1105-gitops
    spec:  
      serviceAccountName: audio-1105-dsp-job
      containers:
      - name: initialize-dsp
        image: quay.io/redhat-ai-dev/utils:latest
        command:
        - /bin/bash
        - -c
        - |
          NS=$(cat /var/run/secrets/kubernetes.io/serviceaccount/namespace)
          oc wait -l statefulset=audio-1105-notebook --for=condition=ready pod --timeout=300s
          oc exec  StatefulSet/audio-1105-notebook -- git clone https://github.com/stephanie-cy/audio-1105
      restartPolicy: Never